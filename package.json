{
  "name": "relationship-wrapped",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/prepare.mjs",
    "dev": "vite",
    "prebuild": "node scripts/prepare.mjs --build",
    "build": "tsc --noEmit && vite build && node scripts/check-css.mjs",
    "prepreview": "node scripts/prepare.mjs",
    "preview": "vite preview",
    "data": "node scripts/build-data.mjs",
    "detect": "impeccable detect index.html src"
  },
  "devDependencies": {
    "@fontsource-variable/schibsted-grotesk": "5.3.0",
    "@fontsource/gloock": "5.3.0",
    "impeccable": "3.6.1",
    "typescript": "5.9.3",
    "vite": "7.1.9"
  }
}
